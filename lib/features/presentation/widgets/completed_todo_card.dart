import 'package:flutter/material.dart';

class CompletedTodoCard extends StatelessWidget {
  const CompletedTodoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
